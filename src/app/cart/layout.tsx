export const metadata = {
  title: "Cart",
  description: "FreshCart - Cart",
};

export default function CartLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children;
}
