export const metadata = {
  title: "Register",
  description: "FreshCart - Register",
};

export default function RegisterLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children;
}
