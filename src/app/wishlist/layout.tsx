export const metadata = {
  title: "Wishlist",
  description: "FreshCart - Wishlist",
};

export default function WishlistLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children;
}
